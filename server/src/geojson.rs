/// Converts OSM data structures into app-specific GeoJSON structures
use geojson::GeoJson::Geometry;

use crate::osm::Neighbor;

// pub fn neighbors_to_feature_collection(neighbors: Neighbor) -> String {
//     let geometry = Geometry::new()
// }
